.banner-phrase {
    height calc(100vh - (100px + (198px / 2)))
    display flex
    align-items center
    z-index -1
    position fixed
    top 100px
    left 0
    right 0
    
    & + * {
        margin-top calc(100vh - (198px / 2))
    }
    
    h1 { font-size: 60px; }
}
